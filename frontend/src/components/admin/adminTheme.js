// Re-exported from the shared theme registry (frontend/src/theme/themeRegistry.js), which now also
// serves the Finance module's "finance" palette. Kept here so the admin portal's existing import
// path (`./adminTheme`) keeps working unchanged.
export { BRAND, BRAND_DARK, adminTheme, createNamedTheme, TONES } from "../../theme/themeRegistry";
